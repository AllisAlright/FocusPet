# FocusPet：AI 陪伴式专注与任务推进系统

## 1. 项目概览

FocusPet 是一款面向轻度拖延、任务分散、难以启动等场景设计的 iOS 专注陪伴应用。它将任务管理、备忘记录、专注计时、历史复盘与 AI 行动建议整合到同一个产品闭环中，目标是帮助用户从“我不知道从哪里开始”过渡到“我现在可以先做一小步”。

FocusPet 不定位为传统待办清单、强压式效率工具或通用聊天机器人。它更像一个低打扰、可解释、可持续的 Pet Agent：用户看到的是宠物陪伴，AI 能力则作为宠物背后的内部能力，在合适的时机提供任务拆解、下一步建议和简短复盘。

核心产品目标：

> 在用户最容易停下来的地方，用低压力、可执行的方式轻轻推一把。

## 2. 目标用户与核心问题

FocusPet 主要服务以下用户：

- 任务很多，但经常不知道应该先做哪一件。
- 想开始学习、工作或生活事务，但任务目标过大、缺乏明确第一步。
- 已经推进过一些事情，但中断后很难重新捡起来。
- 不喜欢被效率工具持续催促，希望获得更温和的外部陪伴。

围绕这些用户，项目将问题收敛为三类。

### 2.1 启动困难

许多任务并非无法完成，而是因为目标表述过大，用户难以判断第一步是什么。例如“准备考试”“整理作品集”“开始健身”都需要先被拆成可执行的小动作。

FocusPet 的处理方式是通过 `split_task` 将自然语言目标拆成 3 到 5 个步骤，并要求步骤足够具体、短小、可立即执行。

### 2.2 决策成本高

当待办列表变长时，用户每次打开 App 都要重新判断“现在该做什么”。这种选择本身会消耗注意力，并进一步加重拖延。

FocusPet 的处理方式是通过 `suggest_next_action` 基于任务状态、进度、截止日期和最近更新时间，给出唯一、低摩擦的下一步，而不是再次抛出多个选项。

### 2.3 持续推进难

很多任务不是一次完成的，而是需要多次进入、暂停、恢复。传统待办工具更擅长记录状态，但不一定能帮助用户在中断后重新进入任务。

FocusPet 通过任务进度、已投入时长、专注记录和历史归档，把“完成”拆成可恢复的小进度。用户不需要每次从零开始，只需要从已有进展继续推进一点。

## 3. 产品方案

FocusPet 的核心方案是“本地优先 App + 后端 Pet Agent + 内部 Skills”。

### 3.1 本地优先的 iOS App

iOS 端承载完整基础功能，包括：

- Home：首页以宠物为主角，提供宠物气泡、聊天入口和常用功能入口。
- Memo：本地备忘录，支持搜索、置顶、软删除、恢复和转待办。
- Tasks：待办管理，记录任务状态、进度、预计耗时、已投入时长和截止日期。
- Focus：专注设置与计时，支持绑定任务或自由专注。
- History：展示已完成任务、未完成记录和专注记录。
- Settings：配置默认宠物、默认场景、默认计时模式和时长。

App 的核心数据优先保存在本地。即使后端不可用，用户仍然可以使用备忘、待办、专注和历史功能。后端主要作为 AI 增强层，而不是 App 的生存依赖。

### 3.2 Pet Agent 作为用户可见的 AI 主体

FocusPet 不把 AI 能力拆成首页上一组裸露工具按钮，而是统一收敛到宠物这一用户可见主体上。

当前 AI 链路：

```text
iOS 宠物聊天输入
-> 后端 Pet Agent 接口
-> 输入安全拦截
-> 意图识别
-> Skill 路由
-> 可选执行内部 Skill
-> 生成宠物口吻回复
-> iOS 展示
-> 涉及任务写入时由用户在 App 内明确确认
```

这种设计让用户感受到的是连续的陪伴体验，而不是在多个 AI 工具之间切换。同时，真实数据写入仍然由前端明确交互控制，避免 LLM 直接修改产品状态。

### 3.3 Skill 化能力设计

后端将 AI 能力拆成稳定的内部 Skill。Skill 负责定义能力边界、输入输出结构和兜底逻辑；Agent 负责决定何时调用 Skill；LLM 负责生成更自然的表达。

当前已实现三个核心 Skill。

#### `split_task`

用途：将用户输入的具体目标拆解为 3 到 5 个可执行步骤。

输入：

- 用户自然语言目标
- 当前宠物类型

输出：

- 可勾选的子任务列表
- 宠物口吻的简短说明

约束：

- 不拆解危险、违法、成人、医疗高风险、过大目标或关系操纵类请求。
- 不直接创建任务，只返回候选步骤。
- 需要用户在 iOS 端确认后，才会写入真实任务数据。

#### `suggest_next_action`

用途：基于当前任务状态给出一个最容易开始的下一步。

输入：

- 任务标题
- 任务状态
- 任务进度
- 截止日期
- 最近更新时间
- 当前宠物类型

输出：

- 一句可立即执行的行动建议
- 推荐原因
- 建议专注时长

约束：

- 默认只给一个建议，降低用户选择成本。
- 建议必须具体、短小、低摩擦。
- 任务排序逻辑由代码控制，LLM 只负责表达优化。

#### `weekly_review`

用途：根据最近专注数据生成简短复盘，帮助用户看到已经发生的进展。

输入：

- 近 7 天专注总时长
- 专注次数
- 深夜专注次数
- 最活跃时段
- 关联最多的任务
- 最长单次专注时长
- 当前宠物类型

输出：

- 总结
- 观察
- 宠物口吻的短评

约束：

- 不进行医疗、心理诊断或过度归因。
- 不制造羞耻感，不把复盘写成压力报告。
- 重点强调可持续的小进展。

## 4. AI 架构设计

FocusPet 当前采用轻量、可控的 Agent 架构。这里的 Agent 不等同于 LLM，而是产品行为决策层。

### 4.1 分层职责

```text
User Input
  -> Input Guard
  -> Intent Detector
  -> Skill Router
  -> Skill Registry
  -> Prompt Builder
  -> LLM Service / Local Fallback
  -> Pet Response
  -> iOS Confirmation / Display
```

各层职责如下：

- `Input Guard`：在调用 Skill 前进行安全与范围判断。
- `Intent Detector`：识别用户意图，例如任务拆解、下一步建议、复盘或普通陪伴。
- `Skill Router`：根据意图选择是否调用内部 Skill。
- `Skill Registry`：统一注册和管理可调用 Skill。
- `Prompt Builder`：拼接宠物人格、任务约束和 Skill 提示词。
- `LLM Service`：通过 DeepSeek 兼容接口生成自然语言表达。
- `Local Fallback`：在 API Key 缺失、LLM 失败或服务不可用时提供本地兜底回复。

### 4.2 决策与表达分离

项目明确区分“确定性决策”和“自然语言表达”：

- 代码负责安全拦截、意图识别、Skill 路由、任务排序、状态变更和数据写入。
- LLM 负责宠物语气、拆解措辞、建议表达和复盘文案。

这样做可以降低不可控输出对产品状态的影响，也便于通过测试覆盖安全边界和核心策略。

### 4.3 安全边界

以下输入不会进入任务拆解或行动规划：

- 自伤、自杀、伤害自己。
- 暴力伤害他人。
- 违法、规避、破解、翻墙绕限制等不适合协助的行为。
- 成人内容或性相关任务。
- 高风险医疗、健康透支类计划。
- 过大且不可直接执行的目标，例如“一夜暴富”“当总统”。
- 以操纵关系结果为目标的请求，例如“让某人喜欢我”。

这些场景会先由确定性规则识别，再生成温和拒绝、转向或陪伴式回复。LLM 可以润色表达，但不能单独决定是否执行 Skill。

## 5. 情绪陪伴与交互设计

FocusPet 的陪伴感不是额外装饰，而是产品降低启动压力的重要组成部分。

### 5.1 宠物人格

当前支持四类宠物人格：

- 兔兔：温柔、细腻、安抚，不急着催促。
- 猫猫：安静、简洁、稍微冷淡但可靠。
- 狗狗：热情、鼓励、行动力强。
- 仓仓：轻快、活泼、有一点小得意。

统一表达约束：

- 不说教。
- 不施压。
- 不制造羞耻感。
- 回复尽量短、轻、具体。
- 行动建议一次只给一个重点。

### 5.2 轻量交互

AI 不作为独立复杂聊天产品存在，而是嵌入在用户原有任务流程中：

- 首页宠物气泡提供轻量陪伴。
- 点击宠物可以切换气泡文案，增强“它在这里”的存在感。
- 宠物聊天承接自然语言输入。
- 拆任务结果以结构化列表展示，用户确认后再转为任务。
- 下一步建议以单句行动形式出现，降低认知负担。

这种方式可以让 AI 帮助用户行动，而不是把用户拉进一段无限聊天。

## 6. 技术实现

### 6.1 iOS 端

技术栈：

- SwiftUI
- 状态驱动 UI
- Codable 本地模型
- 本地 JSON 持久化
- URLSession 调用后端 AI 接口

当前已实现：

- 任务、备忘录、专注、历史、设置等核心模块。
- 首页宠物场景与四个物件入口。
- 宠物聊天记录本地保存。
- 专注计时与任务进度联动。
- 备忘录转待办。
- 后端不可用时的本地兜底体验。

### 6.2 后端

技术栈：

- Python
- FastAPI
- SQLite
- SQLAlchemy
- Pydantic
- DeepSeek 兼容的 OpenAI SDK 调用

当前主要接口：

- `POST /api/v1/ai/agent-message`：Pet Agent 主入口。
- `POST /api/v1/ai/split-task`：任务拆解能力接口，保留兼容与调试用途。
- `POST /api/v1/ai/suggest-next-action`：下一步建议能力接口。
- `POST /api/v1/ai/weekly-review`：周复盘能力接口。
- `GET /health`：健康检查。

后端保留的原因：

- API Key 不进入 iOS App。
- 统一管理 Prompt、安全规则和 Skill 路由。
- 为后续云同步、长期记忆、RAG 和统计分析预留服务端基础。
- 便于通过后端测试覆盖安全拦截、兜底回复和 Skill 合约。

### 6.3 当前数据策略

当前版本采用本地优先策略：

- iOS 端数据是用户体验主链路，支持离线使用。
- 后端 SQLite 主要服务于本地开发、接口调试和后续服务化演进。
- 涉及真实任务数据变更时，必须由用户在 App 内明确确认。

这保证了 AI 后端是增强层，而不是单点依赖。

## 7. 验证与评估指标

FocusPet 的验证重点不是单纯追求模型输出“看起来聪明”，而是验证 AI 是否真的降低了用户开始行动的成本。

建议从以下维度评估：

- 建议相关性：AI 推荐的任务或行动是否符合当前上下文。
- 可执行性：建议是否能在 5 到 15 分钟内开始。
- 重复率：多次请求时是否总是推荐同一任务或同一句话。
- 打扰感：AI 是否在不合适的时机制造压力。
- 确认转化率：拆出的步骤有多少被用户确认加入待办。
- 专注启动率：收到下一步建议后，用户是否进入专注。
- 恢复效率：中断任务是否更容易被重新推进。

当前可持续优化方向：

- 扩充样例矩阵，覆盖更多真实任务输入。
- 优化 Prompt，减少泛化、空泛或说教式输出。
- 调整任务选择策略，平衡稳定性与变化性。
- 完善安全边界测试，避免危险输入进入 Skill。
- 优化首页气泡和动画节奏，降低干扰感。

## 8. 演进路线

### Phase 1：当前 MVP

当前阶段重点是验证“宠物陪伴 + 任务推进”是否成立。

已完成能力：

- 本地优先 iOS App。
- Pet Agent 主链路。
- 输入安全拦截。
- 意图识别与 Skill 路由。
- `split_task`、`suggest_next_action`、`weekly_review` 三个内部 Skill。
- DeepSeek 兼容 LLM 调用。
- LLM 失败时的本地兜底。

### Phase 2：上下文增强

下一阶段可以重点增强上下文质量：

- 统一前端任务数据与后端任务数据。
- 引入更稳定的用户行为摘要。
- 增强周复盘与任务恢复建议。
- 建立更多任务拆解模板与提示词回归样例。
- 完善 App Store 隐私说明与生产部署准备。

### Phase 3：长期智能化

更长期的演进方向包括：

- RAG：引入任务拆解模板库、专注策略库和产品知识库。
- Long-term Memory：在用户授权前提下记录长期偏好和任务模式。
- MCP 或等价上下文协议：为 Agent 提供更清晰、可控的实时任务状态和专注历史。
- 系统集成：接入日历、提醒事项或推送通知。
- 订阅与多端同步：在产品验证后再考虑商业化与跨端能力。

## 9. 项目亮点

### 9.1 产品层面

- 将 AI 从“聊天入口”收敛为“宠物陪伴主体”，降低工具感。
- 用单句建议和结构化拆解降低启动成本，而不是增加新的管理负担。
- 本地优先设计保证核心功能不依赖后端可用性。
- 通过宠物人格控制语气，使效率工具更温和、更可持续。

### 9.2 技术层面

- 使用 Pet Agent 统一承接意图识别、安全拦截、Skill 调度和回复生成。
- 将 Skill 设计为可测试、可扩展的业务能力模块。
- 将确定性逻辑与 LLM 表达分离，降低不可控输出风险。
- 后端集中管理 API Key、Prompt 和安全边界，iOS 端只负责明确交互和本地状态。
- 建立 LLM 失败兜底机制，保证关键体验不被外部模型服务完全阻断。

### 9.3 AI Engineering 层面

- 使用 Prompt Engineering 约束输出结构、语气和任务边界。
- 使用 Rule-based Agent 保证第一期行为可预测、可测试。
- 使用 Codex 辅助完成前后端迭代、文档整理和测试补充。
- 通过样例输出和安全测试持续校准 AI 行为。

## 10. 总结

FocusPet 的核心不是“让 AI 聊得更多”，而是“让 AI 在用户需要开始行动时提供刚好够用的帮助”。

项目最终形成了一个较完整的端到端闭环：

```text
记录想法
-> 转化任务
-> 拆成小步
-> 推荐下一步
-> 进入专注
-> 沉淀历史
-> 生成复盘
-> 再次恢复推进
```

从产品角度看，FocusPet 关注的是轻压力、可恢复和持续推进。从技术角度看，它通过本地优先 App、后端 Pet Agent、确定性安全规则和 Skill 化能力，把 LLM 放在可控的位置上，让 AI 成为任务系统的增强层，而不是替代产品逻辑本身。

一句话概括：

> FocusPet 是一个陪用户重新开始的行动型 AI，而不是一个单纯聊天的 AI。
