# FocusPet 产品与 Pet Agent 口径

本文件是当前 FocusPet 的产品和 Agent 统一口径。它替代旧的 Agent v2 草稿，也替代早期“把 AI 能力拆成多个裸露工具入口”的设计。

以后如果产品、后端或提示词发生变化，优先对齐这个文件。

## 产品定位

FocusPet 是一款 iOS 专注陪伴应用，服务那些任务很多、容易拖延、经常不知道从哪里重新开始的用户。

它不是：

- 传统待办清单
- 强压式效率工具
- 通用聊天机器人
- 心理治疗或医疗产品
- 万能 AI 助理

它更像一个温柔的专注空间：用户可以把碎片想法记下来，把事情变成可恢复的小进度，在宠物陪伴下先推进一点。

核心感受应该是：

```text
我不是被逼着马上做完所有事。
我只是有一个小伙伴陪我重新开始。
```

## 当前 App 形态

当前 iOS 模块包括：

- Home：首页，宠物是主角，提供四个物件入口和宠物聊天入口。
- Memo：备忘录，本地文本记录，支持搜索、置顶、软删除、恢复、转待办。
- Tasks：待办事项，基于进度、已投入时长、预估耗时和截止日期推进。
- Focus：专注设置和专注计时，支持绑定任务或自由专注。
- History：历史记录，展示已完成任务、未完成记录和专注记录。
- Settings：设置默认宠物、默认场景、默认计时模式和时长。

当前主导航是底部 Tab：

- 首页
- 待办
- 专注
- 历史
- 设置

备忘录不是底部 Tab，而是首页强入口。原因是备忘录更像“快速承接碎片想法”的入口，不一定是用户每天反复浏览的一级目的地。

## 当前首页口径

首页已经从“功能入口页”改成“宠物陪伴与 Agent 输入页”。

这意味着：

- 宠物必须是首屏情绪和交互中心。
- 四个入口服务于效率，但不能让首页变成冰冷控制台。
- 宠物气泡可以提供轻量陪伴文案。
- 点击宠物可以切换气泡文案，增强“它在这里”的感觉。
- “帮我拆任务”“我先做哪个”等能力可以出现在聊天或快捷入口里，但不能喧宾夺主。

## 当前 AI / Agent 形态

当前使用 Pet Agent。

用户看到的是宠物，不是一个工具列表。skill 是宠物背后的内部能力。

当前链路：

```text
iOS 宠物聊天输入
-> 后端 Pet Agent 接口
-> 输入安全拦截
-> 意图识别
-> skill 路由
-> 可选执行内部 skill
-> 生成宠物口吻回复
-> iOS 展示
-> 如果涉及任务写入，由用户在 App 内明确确认
```

当前后端 skill：

- `split_task`：把适合拆解的具体任务拆成 3 到 5 个小步骤。
- `suggest_next_action`：根据当前任务上下文推荐一个低压力下一步。
- `weekly_review`：根据近期专注数据生成简短复盘。

当前宠物人格：

- 兔兔：温柔、细腻、安抚，不急着催。
- 猫猫：安静、简洁、稍微冷淡但可靠。
- 狗狗：热情、鼓励、行动力强。
- 仓仓：轻快、活泼、有一点小得意。

## 安全与边界

安全策略必须分层处理，不能完全交给 LLM 临场判断。

当前原则：

- 先用确定性代码判断输入是否危险或不适合执行 skill。
- 再让 LLM 负责更自然、更像宠物的表达。
- LLM 可以生成安抚、拒绝、转向的措辞。
- LLM 不能单独决定是否创建任务、拆危险任务、删除数据或改变任务状态。
- iOS 端只有在用户明确点击确认后，才执行真实数据写入。

以下输入不能进入任务拆分或行动规划：

- 自伤、自杀、伤害自己
- 暴力伤害他人
- 违法、规避、翻墙绕限制等不适合协助的行为
- 成人内容或性相关任务
- 高风险医疗、健康操作
- “赚一百万”“当总统”这类过大、不可直接执行的目标
- “让某人喜欢我”“操纵关系结果”这类关系控制目标

这些情况下，宠物应该温和拒绝或转向可做的小事，而不是把问题拆成步骤。

## 已实现

当前已经实现：

- SwiftUI iOS 主体结构
- 本地 JSON 持久化
- 任务、备忘录、专注、设置、宠物聊天记录保存
- 本地兜底数据
- 宠物首页场景
- 首页宠物点击切换 20 条气泡文案
- 宠物聊天记录保留
- 后端 FastAPI 服务
- 后端 DeepSeek 兼容调用
- Pet Agent 人格和提示词层
- skill 前置安全拦截
- 意图识别和 skill 路由
- 内部 skill 注册表和轻量编排
- LLM 失败时的本地兜底回复
- 后端安全拦截、提示词边界和兜底逻辑测试

## 暂未实现

当前还没有实现：

- 账号系统
- 云同步
- 多端同步
- 生产环境部署
- 推送通知
- App Store 隐私说明完整页面
- 真实白噪音音频资源
- RAG 检索增强
- 长期用户记忆服务
- 订阅或 App 内购买
- 日历、提醒事项系统集成

有些规划文档里可能还会提到 Memory、RAG、统计分析等未来能力，但它们不是当前可用产品的一部分。

## 为什么采用这种形态

当前架构刻意让 iOS App 不依赖 AI 后端也能正常使用。

这样做有几个原因：

- DeepSeek API Key 不能放进 iOS App。
- 提示词、安全规则和 skill 路由放在后端更容易统一管理。
- 安全判断应该先由稳定代码完成，而不是全靠模型生成。
- 用户的任务、备忘录、专注记录属于核心功能，不能因为后端不可用就失效。
- Pet Agent 应该增强陪伴感，而不是替代整个产品逻辑。

## 后续改动规则

后续开发请遵守这些规则：

- 宠物始终是用户看到的主 Agent。
- 不把 skill 做成首页上一堆裸露大按钮。
- 宠物回复保持短、轻、温和，避免大段说教。
- 安全、路由、数据写入用确定性代码控制。
- LLM 负责表达质量，不负责产品状态。
- 涉及真实任务数据变化时，必须让用户明确确认。
- 首页不要变成压力仪表盘。
- 危险、违法、成人、医疗风险、过大目标、关系操纵类输入，都要从任务 skill 中排除。
