# FocusPet 产品与 Pet Agent 设计准则

## 1. 文档定位

本文档定义 FocusPet 当前阶段的产品口径、Pet Agent 设计原则、AI 能力边界和后续开发约束。它用于统一产品、前端、后端和提示词相关改动的判断标准。

当 README、技术文档、提示词草稿或历史规划与本文档不一致时，以本文档为准。后续如果产品形态、Agent 链路、Skill 能力或安全策略发生变化，应优先更新本文档，再同步到其他文档。

本文档替代以下早期方向：

- 将 AI 能力拆成多个裸露工具入口的设计。
- 将 Agent 设计为通用聊天机器人的设计。
- 将 LLM 作为产品状态决策者的设计。
- 旧版 Agent v2 草稿中尚未落地或已不符合当前产品形态的设想。

## 2. 产品定位

FocusPet 是一款 iOS 专注陪伴应用，面向任务很多、容易拖延、经常不知道如何重新开始的用户。

它的目标不是让用户一次性完成所有事情，而是帮助用户把分散想法承接下来，把模糊目标拆成可恢复的小进度，并在宠物陪伴下先推进一个低压力步骤。

FocusPet 不是：

- 传统待办清单。
- 强压式效率工具。
- 通用聊天机器人。
- 心理治疗或医疗产品。
- 万能 AI 助理。

FocusPet 的核心体验应当是：

```text
我不是被逼着马上做完所有事。
我只是有一个小伙伴陪我重新开始。
```

产品关键词：

- 低压力：不给用户制造羞耻感或过度催促。
- 可恢复：任务中断后可以从已有进度继续。
- 可解释：AI 建议应能说明基本原因，不是黑箱式命令。
- 可控：涉及真实数据变化时必须由用户确认。
- 本地优先：核心功能不依赖 AI 后端持续可用。

## 3. 当前产品形态

当前 FocusPet 采用“本地优先 iOS App + 可选 AI 后端增强”的形态。

### 3.1 iOS 模块

当前 iOS 端包含以下模块：

- Home：首页，宠物是主角，提供宠物气泡、宠物聊天入口和四个常用物件入口。
- Memo：备忘录，本地文本记录，支持搜索、置顶、软删除、恢复和转待办。
- Tasks：待办事项，基于任务状态、进度、已投入时长、预估耗时和截止日期推进。
- Focus：专注设置与专注计时，支持绑定任务或自由专注。
- History：历史记录，展示已完成任务、未完成记录和专注记录。
- Settings：设置默认宠物、默认场景、默认计时模式和默认时长。

### 3.2 主导航

当前主导航为底部 Tab：

- 首页
- 待办
- 专注
- 历史
- 设置

备忘录不是底部 Tab，而是首页强入口。原因是 Memo 的产品职责是快速承接碎片想法，不一定是用户每天反复浏览的一级目的地。首页保留 Memo 强入口，可以让用户在产生想法时快速记录，再在合适的时候转为任务。

### 3.3 本地优先策略

iOS 端应独立支撑以下核心功能：

- 任务记录与状态维护。
- 备忘录记录与转待办。
- 专注计时与专注记录。
- 历史记录查看。
- 设置项保存。
- 宠物聊天记录保存。

后端不可用时，以上能力不应失效。AI 后端是体验增强层，不是 App 基础功能的单点依赖。

## 4. 首页设计口径

首页已经从“功能入口页”调整为“宠物陪伴与 Agent 输入页”。

首页设计必须遵守以下原则：

- 宠物是首屏情绪与交互中心，不是装饰图标。
- 四个入口用于快速进入效率场景，但不应把首页变成冰冷控制台。
- 宠物气泡承担轻量陪伴、恢复进入和温和提示的职责。
- 点击宠物可以切换气泡文案，增强“宠物在这里陪着我”的存在感。
- “帮我拆任务”“我先做哪个”等能力可以通过聊天或轻量入口出现，但不能喧宾夺主。
- 首页不应发展成压力仪表盘，不应堆叠大量指标、红色警告或强提醒。

首页的目标不是展示所有功能，而是让用户愿意重新打开 App，并从一个小动作开始。

## 5. Pet Agent 形态

当前使用的是 Pet Agent，而不是通用聊天助手。

用户看到的是宠物；Skill 是宠物背后的内部能力。产品不应把 Skill 直接暴露成一组大按钮，也不应让用户感到自己在操作复杂 AI 工具箱。

### 5.1 当前链路

```text
iOS 宠物聊天输入
-> 后端 Pet Agent 接口
-> 输入安全拦截
-> 意图识别
-> Skill 路由
-> 可选执行内部 Skill
-> 生成宠物口吻回复
-> iOS 展示
-> 涉及任务写入时，由用户在 App 内明确确认
```

### 5.2 分层职责

Pet Agent 是产品行为决策层，LLM 是表达生成层。

代码负责：

- 输入安全拦截。
- 意图识别。
- Skill 路由。
- 任务选择和排序。
- 数据写入条件判断。
- 本地兜底行为。
- 测试可覆盖的安全和业务规则。

LLM 负责：

- 宠物语气表达。
- 拆任务步骤的自然语言措辞。
- 下一步建议的温和表达。
- 周复盘的简短总结。
- 拒绝、转向和陪伴文案的润色。

LLM 不负责：

- 创建、删除、暂停、恢复或完成任务。
- 判断是否写入真实数据。
- 绕过安全规则调用 Skill。
- 单独决定产品状态。
- 对医疗、法律、心理等高风险问题给出专业结论。

## 6. 当前 Skill 体系

当前后端 Skill 是 Pet Agent 的内部业务能力，不是用户直接操作的独立工具。

### 6.1 `split_task`

职责：将适合拆解的具体任务拆成 3 到 5 个小步骤。

适用场景：

- 用户表达了一个较明确但难以启动的任务。
- 用户请求“帮我拆一下”“分成几步”“不知道怎么开始”。

输出要求：

- 步骤具体、短小、可执行。
- 不生成过长解释。
- 不直接写入任务列表。
- 由 iOS 端展示候选步骤，并由用户确认后再创建任务。

### 6.2 `suggest_next_action`

职责：根据当前任务上下文推荐一个低压力下一步。

适用场景：

- 用户询问“我现在该做什么”“先做哪个”“下一步是什么”。
- 用户当前有多个未完成任务，或存在进行中、临期、逾期、暂停任务。

输出要求：

- 默认只给一个建议。
- 建议应能在 5 到 15 分钟内开始。
- 说明简短原因，避免命令式语气。
- 推荐任务与任务排序逻辑由代码控制，LLM 只优化表达。

### 6.3 `weekly_review`

职责：根据近期专注数据生成简短复盘。

适用场景：

- 用户主动请求复盘。
- 产品后续在合适入口展示近 7 天专注总结。

输出要求：

- 总结近期投入，而不是评判用户表现。
- 提供一条温和观察。
- 不做医疗、心理诊断或人格归因。
- 不制造内疚感或焦虑感。

## 7. 宠物人格规范

当前支持四类宠物人格：

- 兔兔：温柔、细腻、安抚，不急着催促。
- 猫猫：安静、简洁、稍微冷淡但可靠。
- 狗狗：热情、鼓励、行动力强。
- 仓仓：轻快、活泼、有一点小得意。

所有宠物必须遵守统一表达边界：

- 回复保持短、轻、温和。
- 不说教。
- 不施压。
- 不制造羞耻感。
- 不用夸张承诺保证结果。
- 不把用户的问题归因为懒惰或意志力差。
- 行动建议一次只聚焦一个小动作。

宠物人格只能影响语气和表达方式，不能改变安全边界、数据写入规则或 Skill 调用规则。

## 8. 安全与边界

安全策略必须分层处理，不能完全交给 LLM 临场判断。

当前原则：

- 先由确定性代码判断输入是否危险或不适合执行 Skill。
- 再让 LLM 负责更自然、更像宠物的表达。
- LLM 可以生成安抚、拒绝、转向的措辞。
- LLM 不能单独决定是否创建任务、拆危险任务、删除数据或改变任务状态。
- iOS 端只有在用户明确点击确认后，才执行真实数据写入。

以下输入不能进入任务拆分或行动规划：

- 自伤、自杀、伤害自己。
- 暴力伤害他人。
- 违法、规避、破解、翻墙绕限制等不适合协助的行为。
- 成人内容或性相关任务。
- 高风险医疗、健康操作或明显透支身体的计划。
- 过大且不可直接执行的目标，例如“赚一百万”“当总统”“一夜暴富”。
- 关系操纵类目标，例如“让某人喜欢我”“保证挽回某人”。

这些情况下，宠物应温和拒绝、转向安全话题或建议一个普通生活中的小动作，而不是把请求拆成步骤。

## 9. 当前已实现范围

当前已经实现：

- SwiftUI iOS 主体结构。
- 底部 Tab 主导航。
- Home、Memo、Tasks、Focus、History、Settings 模块。
- 本地 JSON 持久化。
- 任务、备忘录、专注记录、设置和宠物聊天记录保存。
- 本地兜底数据。
- 宠物首页场景。
- 首页宠物点击切换 20 条气泡文案。
- 宠物聊天记录保留。
- 后端 FastAPI 服务。
- 后端 SQLite 与 SQLAlchemy 基础数据层。
- DeepSeek 兼容调用。
- Pet Agent 人格和提示词层。
- Skill 前置安全拦截。
- 意图识别和 Skill 路由。
- 内部 Skill 注册表和轻量编排。
- `split_task`、`suggest_next_action`、`weekly_review` 三个内部 Skill。
- LLM 失败或 API Key 缺失时的本地兜底回复。
- 后端安全拦截、提示词边界和兜底逻辑测试。

## 10. 当前未实现范围

当前尚未实现：

- 账号系统。
- 云同步。
- 多端同步。
- 生产环境部署。
- 推送通知。
- App Store 隐私说明完整页面。
- 真实白噪音音频资源。
- RAG 检索增强。
- 长期用户记忆服务。
- 订阅或 App 内购买。
- 日历、提醒事项等系统集成。
- 生产级鉴权、限流、监控和计费。

有些规划文档中可能还会提到 Memory、RAG、统计分析、外部工具接入等未来能力。除非在本文档和当前代码中明确标记为已实现，否则它们都应视为后续规划，不应在对外说明中写成当前可用能力。

## 11. 采用当前形态的原因

当前架构刻意让 iOS App 不依赖 AI 后端也能正常使用。

主要原因：

- DeepSeek API Key 不能放进 iOS App。
- Prompt、安全规则和 Skill 路由放在后端更容易统一管理和测试。
- 安全判断应先由稳定代码完成，而不是完全依赖模型生成。
- 用户的任务、备忘录、专注记录属于核心数据，不能因为后端不可用而失效。
- Pet Agent 应增强陪伴感和任务推进能力，而不是替代 App 的产品逻辑。
- 第一阶段更需要可预测、可验证的行为，而不是复杂自治。

## 12. 后续改动规则

后续开发请遵守以下规则。

### 12.1 产品规则

- 宠物始终是用户看到的主 Agent。
- 不把 Skill 做成首页上一堆裸露大按钮。
- 首页不做压力仪表盘。
- 备忘录继续承担碎片想法承接入口。
- 任务推进强调“先做一点”，而不是强迫用户完成全部。

### 12.2 AI 规则

- 宠物回复保持短、轻、温和，避免大段说教。
- 安全、路由、数据写入由确定性代码控制。
- LLM 负责表达质量，不负责产品状态。
- 危险、违法、成人、医疗风险、过大目标、关系操纵类输入，都必须从任务 Skill 中排除。
- 新增 Skill 必须定义清楚输入、输出、适用场景、拒绝场景和兜底策略。

### 12.3 数据规则

- 涉及真实任务数据变化时，必须让用户明确确认。
- 不允许 LLM 直接修改本地数据或数据库状态。
- 后端返回的结构化结果应被视为候选建议，而不是已执行操作。
- 用户核心数据优先保障本地可用性。

### 12.4 文档规则

- 若新增或修改核心产品能力，应同步更新本文档。
- 若新增后端 Skill，应同步更新 `Backend/app/skills/README.md` 和相关 Skill 文档。
- 若调整 Agent 链路，应同步更新 `Docs/Technical_Agent_Architecture.md`。
- 对外展示文档应区分“已实现”和“规划中”，避免把未来路线写成当前能力。
